# Copy to .env and fill in. NEVER commit the real .env (it's gitignored).
# All of these come from YOUR Stripe account / host — the app code never holds them.

STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me_after_creating_the_webhook

# The web origin allowed to call this backend (your GitHub Pages site).
ALLOWED_ORIGIN=https://4hhmt57fpf-blip.github.io

# Native Apple Pay merchant id (create in the Apple Developer portal, register in Stripe).
APPLE_MERCHANT_ID=merchant.com.ante.app

# Anthropic key for the in-app Help chatbot (/ai-chat). Get one at console.anthropic.com.
ANTHROPIC_API_KEY=sk-ant-replace_me
